enum CompanyType {
  automobile,
  recycling,
  manufacturing,
  others,
}

enum HierarchyMode {
  simple,
  advanced,
}

enum MaterialStatus {
  pending,
  approved,
  rejected,
}

enum ApprovalStatus {
  pending,
  approved,
  rejected,
}
